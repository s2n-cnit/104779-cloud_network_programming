# Setup

1. uv init
2. uv add click
3. uv add loguru
4. uv add prompt_toolkit
5. uv add pyyaml

# Client 1

1. uv run python main.py client -c alex.yaml

# Client 2

2. uv run python main.py client -c alex.yaml

# Server

3. uv run python main.py server -c server.yaml
